[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: requires real Supabase / Redis (skipped in unit runs)
    asyncio: requires pytest-asyncio (skipped if not installed)
